apiName: "Fleur's API"

assets:
  mappings:
    /assets: /
  overrides:
    /: ../IPRWC-webshop-frontend/dist/webshop-client/

server:
  rootPath: /api/*
  adminContextPath: /admin
  applicationConnectors:
    - type: http
      port: 8080
  adminConnectors:
    - type: http
      port: 8081